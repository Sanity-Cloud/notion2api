# Notion2API Orphan Branch Fan-In Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconcile every branch not merged into canonical `main`, adopt only unique still-applicable changes, and close the integration with test, push, restart, and live-health evidence.

**Architecture:** Treat `X:\Code\notion2api` / `sanity-pr/main` as canonical. Perform all mutation in the isolated `integrate/orphan-fanin-20260816` worktree. Use patch-equivalence plus semantic inspection to separate duplicate/superseded lineage from unique work; preserve any branch that cannot be proven absorbed.

**Tech Stack:** Git, Python 3, pytest, PowerShell 7, Notion2API backend/MCP runtime.

## Global Constraints

- Canonical repository: `X:\Code\notion2api`; canonical branch: `main`; canonical publication remote: `sanity-pr/main`.
- Do not force-push, reset, clean, overwrite, or delete unabsorbed branches/worktrees.
- Treat merge-only ancestry as non-substantive unless it carries a unique patch.
- Child/adopted changes must remain within the existing A2 authority ceiling.
- Full fan-in requires a clean diff check, compile check, full pytest pass, pushed commit identity, controlled restart, and live health build-commit match.

---

### Task 1: Establish clean baseline

**Files:**
- Modify: none
- Test: repository test suite

**Interfaces:**
- Consumes: `integrate/orphan-fanin-20260816` at current `main`.
- Produces: baseline test receipt and immutable fan-in base commit.

- [ ] Run `python -m pytest -q` in the isolated worktree and require zero failures.
- [ ] Record `git status --short --branch`, `git rev-parse HEAD`, and `git diff --check`.

### Task 2: Classify all orphaned branch patches

**Files:**
- Modify: none
- Test: Git patch-equivalence/ancestry checks

**Interfaces:**
- Consumes: every local branch reported by `git branch --no-merged main`.
- Produces: branch-by-branch classification of absorbed, superseded, unique-applicable, or unresolved.

- [ ] Enumerate unmerged branches and each branch's commits unique from `main`.
- [ ] Use `git cherry main <branch>`, `git patch-id`, ancestry, touched-file diffs, and commit messages to detect duplicates and merge-only lineage.
- [ ] Inspect unique patches against current `main`; classify semantic collisions as unresolved rather than guessing.

### Task 3: Adopt unique still-applicable changes

**Files:**
- Modify: only files touched by validated unique patches
- Test: focused tests for each adopted subsystem

**Interfaces:**
- Consumes: Task 2 unique-applicable patch set.
- Produces: one or more narrow integration commits preserving original commit provenance in commit messages.

- [ ] Cherry-pick only patches that apply cleanly and remain semantically valid.
- [ ] For conflicts, compare current implementation with orphan patch and resolve toward current contracts; do not restore superseded behavior.
- [ ] Run focused tests after each subsystem adoption and commit each accepted unit independently.

### Task 4: Fan-in verification

**Files:**
- Modify: none except required conflict resolutions already covered by Task 3
- Test: all tracked tests

**Interfaces:**
- Consumes: integrated candidate branch.
- Produces: acceptance evidence for promotion to `main`.

- [ ] Run `git diff --check` and Python compile validation.
- [ ] Run `python -m pytest -q`; require zero failures.
- [ ] Re-run `git branch --no-merged HEAD` and document any intentionally retained branches with reasons.

### Task 5: Promote, publish, and verify runtime

**Files:**
- Modify: canonical branch pointer only; runtime receipt generated by existing restart tooling
- Test: full suite on promoted `main`, controlled restart, live MCP health

**Interfaces:**
- Consumes: green fan-in candidate.
- Produces: pushed `sanity-pr/main`, restart receipt, live build-commit receipt, and safe branch cleanup.

- [ ] Fast-forward/merge candidate into local `main` without rewriting history.
- [ ] Re-run full tests on the promoted `main` tree.
- [ ] Push `main` to `sanity-pr` without force and verify local/remote commit equality.
- [ ] Restart backend `8120` and MCP `8130` via the governed PowerShell 7 restart script.
- [ ] Call live MCP health and require HTTP 200 plus `history.build_commit == git rev-parse main`.
- [ ] Delete only local branches proven fully absorbed; preserve unresolved or externally owned worktrees/branches.
