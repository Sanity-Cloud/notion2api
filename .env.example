# Notion-AI Configuration Template
# Copy this file to .env and fill in actual values.

# ----------------------------------------------------------
# 1. Notion Accounts
# ----------------------------------------------------------
# Core credential pool: JSON array of Notion account configurations.
# Must include: token_v2, space_id, user_id.
# Multiple accounts enable load balancing and rate-limit rotation.
# Recommended: run `python login.py` locally to create or refresh account configuration.
# ----------------------------------------------------------
NOTION_ACCOUNTS='[{"profile_name":"default","token_v2":"your_token","space_id":"your_space","user_id":"your_uid","space_view_id":"your_view","user_name":"your_name","user_email":"your_email"}]'

# ----------------------------------------------------------
# 2. FastAPI Service & Security
# ----------------------------------------------------------
# API_KEY: Bearer token required by clients calling /v1/chat/completions.
# Leave empty to disable auth. This is not recommended for production.
API_KEY=your-api-key-here

# HOST: service bind address. 0.0.0.0 allows external network access.
HOST=0.0.0.0

# PORT: internal service port.
PORT=8000

# ALLOWED_ORIGINS: comma-separated CORS origins.
# Use concrete origins in production. Avoid wildcard CORS in production.
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ----------------------------------------------------------
# 3. Application Mode
# ----------------------------------------------------------
# APP_MODE: application runtime mode.
# - standard: full context, thinking/search support, no database; recommended.
# - lite: single-turn Q&A, no memory, no database.
# - heavy: full session management; requires SQLite + SILICONFLOW_API_KEY.
APP_MODE=standard

# ----------------------------------------------------------
# 4. Docker Configuration
# ----------------------------------------------------------
# HOST_PORT: host-exposed port used by docker-compose.
HOST_PORT=8000

# ----------------------------------------------------------
# 5. Data Persistence (SQLite)
# ----------------------------------------------------------
# DB_PATH: chat-history database file path.
# Docker deployments override this to /app/data/conversations.db in docker-compose.yml.
# For local development, use a relative path such as ./data/conversations.db.
DB_PATH=./data/conversations.db

# ----------------------------------------------------------
# 6. Advanced Configuration (Optional)
# ----------------------------------------------------------
# SILICONFLOW_API_KEY: optional SiliconFlow backup service key.
SILICONFLOW_API_KEY=

# NOTION_CLIENT_VERSION: Notion web client version. Update if Notion changes protocol behavior.
NOTION_CLIENT_VERSION=23.13.20260228.0625

# Log Level: DEBUG, INFO, WARNING, ERROR, CRITICAL.
LOG_LEVEL=INFO

# NOTION_PERSIST_THREADS: Default persistence behavior for Notion AI threads.
# - true: Always persist and show threads in Notion AI chat history.
# - false: Delete ephemeral threads after streaming (default for standard/lite modes).
NOTION_PERSIST_THREADS=false

# Optional durable state file for explicit Auto/Pinned account selection.
# The file stores only the selected profile name and mode; it never stores credentials.
# NOTION_ACCOUNT_SELECTION_STATE=./data/account-selection.json

# External URL confirmations (Notion connections.web.loadPage / urlSafety).
# Notion2API auto-approves these as protocol continuations; access control is external.
# EXTERNAL_URL_APPROVAL_POLICY=allow_all|manual
EXTERNAL_URL_APPROVAL_POLICY=allow_all
AUTO_CONTINUE_EXTERNAL_URL_CONFIRMATIONS=true

# Timezone.
TZ=UTC

# SanityCloud canonical governance and knowledge-routing contract.
# Sanity Management / Sanity-Cloud-InScene is the sole default authority.
SANITYCLOUD_DEFAULT_WORKSPACE=sanity-management
SANITYCLOUD_ENABLED_WORKSPACES=sanity-management
SANITYCLOUD_GOVERNANCE_CONTRACT_VERSION=sanitycloud-governance-v2-sanity-management
SANITYCLOUD_NOTION_WORKSPACE_ID=fe8b13aa-3ad2-811e-8292-0003b78a02f9
SANITYCLOUD_NOTION_TEAMSPACE_ID=3acb13aa-3ad2-8176-9ff3-004220d4868f
SANITYCLOUD_GOVERNANCE_AUTHORITY_PAGE_ID=3acb13aa-3ad2-816c-b6ec-d4930a87e12e
SANITYCLOUD_DOCUMENTED_OUTPUT_PARENT_PAGE_ID=3acb13aa-3ad2-8161-b851-f9bb30c31ecc
SANITYCLOUD_PROCEDURAL_FEEDBACK_PARENT_PAGE_ID=3acb13aa-3ad2-813f-81c9-c659c579c093

# Durable secondary-runtime state. Keep these outside the repository checkout.
NOTION2API_HIVE_RUNTIME_DB_PATH=X:\MCP\state\notion2api-hive\hive_runtime.sqlite3
MCP_NOTION2API_SESSION_STATE=X:\MCP\state\notion2api-hive\mcp_sessions.json
MCP_NOTION2API_CHAT_JOB_STATE=X:\MCP\state\notion2api-hive\mcp_chat_jobs.json
MCP_NOTION2API_CHAT_JOB_DB=X:\MCP\state\notion2api-hive\mcp_chat_jobs.sqlite3
